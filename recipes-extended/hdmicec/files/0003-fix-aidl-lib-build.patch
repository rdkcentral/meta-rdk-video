diff --git a/build.sh b/build.sh
index 8a781f7..a760091 100755
--- a/build.sh
+++ b/build.sh
@@ -144,7 +144,8 @@ generate_hal_interfaces()
 build_aidl_libs()
 {
     pushd ${AIDL_LIB_DIR}
-    make -j`nproc` TARGET=$TARGET VARIANT=CPP
+    make -j`nproc` TARGET=$TARGET VARIANT=CPP \
+           CC="${CC}" CXX="${CXX}"
     if [ $? -ne 0 ]; then
         echo -e "${BOLD}╔══════════════════════════════════════════════╗${RESET}"
         echo -e "${BOLD}* ${RED}Failed to build the AIDL Lib. Exiting.${RESET}       ${BOLD}*${RESET}"
