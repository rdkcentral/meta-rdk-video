diff --git a/Source/WPEFramework/PluginServer.cpp b/Source/WPEFramework/PluginServer.cpp
index fa723305..1fb7d56e 100644
--- a/Source/WPEFramework/PluginServer.cpp
+++ b/Source/WPEFramework/PluginServer.cpp
@@ -1093,8 +1093,12 @@ POP_WARNING()
         std::vector<PluginHost::ISubSystem::subsystem> externallyControlled;
         ServiceMap::Iterator iterator(_services.Services());
 
-#if 0
+#if 1
         // Load the metadata for the subsystem information..
+	struct timespec ts;
+        clock_gettime(CLOCK_MONOTONIC, &ts);
+        SYSLOG(Logging::Startup, (Core::Format(_T("Start of LoadMetadata: %llu"), (uint64_t)ts.tv_sec * 1000000000ULL + ts.tv_nsec)));
+        printf("Start of LoadMetadata: %llu", (uint64_t)ts.tv_sec * 1000000000ULL + ts.tv_nsec);
         while (iterator.Next() == true)
         {
             iterator->LoadMetadata();
@@ -1112,6 +1116,9 @@ POP_WARNING()
                 }
             }
         }
+        clock_gettime(CLOCK_MONOTONIC, &ts);
+        SYSLOG(Logging::Startup, (Core::Format(_T("End of LoadMetadata: %llu"), (uint64_t)ts.tv_sec * 1000000000ULL + ts.tv_nsec)));
+        printf("End of LoadMetadata: %llu", (uint64_t)ts.tv_sec * 1000000000ULL + ts.tv_nsec);
 #endif
 
         _controller->Activate(PluginHost::IShell::STARTUP);
diff --git a/Source/plugins/MetaData.h b/Source/plugins/MetaData.h
index 206b4f6d..6f46f8bf 100644
--- a/Source/plugins/MetaData.h
+++ b/Source/plugins/MetaData.h
@@ -447,9 +447,14 @@ namespace Plugin {
             , _precondition(precondition)
             , _termination(termination)
             , _control(control) {
+	    struct timespec ts;
+            clock_gettime(CLOCK_MONOTONIC, &ts);
+            printf("Start Time for Metadata Initializing %s = %llu ns\n", _Id.c_str(),  (uint64_t)ts.tv_sec * 1000000000ULL + ts.tv_nsec);
             ASSERT(Core::System::ROOT_META_DATA == nullptr);
             Core::System::ROOT_META_DATA = this;
             Core::ServiceAdministrator::Instance().Register(this, &_factory);
+            clock_gettime(CLOCK_MONOTONIC, &ts);
+            printf("End Time for Metadata Initializing %s = %llu ns\n", _Id.c_str(),  (uint64_t)ts.tv_sec * 1000000000ULL + ts.tv_nsec);
         }
         ~Metadata() {
             Core::ServiceAdministrator::Instance().Unregister(this, &_factory);
