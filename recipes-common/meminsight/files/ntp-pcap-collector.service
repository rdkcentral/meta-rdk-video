[Unit]
Description=NTP Pktrate collector
Before=systemd-timesyncd.service

[Service]
Type=simple
ExecStart=/bin/sh -c '/lib/rdk/ntp-poll-interval.sh >> /tmp/pktrate.log'

[Install]
WantedBy=multi-user.target
